plugin: amazon.aws.aws_ec2
strict: False

regions:
  - us-east-1

filters:
  instance-state-name: running
  "tag:Name": backend

compose:
  ansible_host: private_ip_address

hostnames:
  - private-ip-address

keyed_groups:
  - key: tags.Name
    prefix: dynamic
    separator: "_"
